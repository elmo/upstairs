<%= simple_form_for([:manage, @building, @unit] ) do |f| %>

  <%= f.error_notification %>

  <div class="form-inputs">
    <%= f.input :name, prompt: 'Enter the unit name/number' %>
  </div>

  <div class="form-actions">
    <%= f.button :submit,  'save' %>
  </div>

<% end %>
