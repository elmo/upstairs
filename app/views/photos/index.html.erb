<h1>Photos</h1>

<% if @photos.empty? %>
<p> There are no photos or this building. Fee free to add some.</p>
<% end %>

<% @photos.each do |photo| %>
   <%= cl_image_tag(photo.path, { width: '420', crop: :scale}) %>
<% end %>

<br />

<% @community.photos.each_with_index do |photo, i| -%>
  <%= link_to community_photos_path(page: i+1 ) do -%>
   <%= cl_image_tag(photo.path, { width: '80', crop: :scale}) %>
  <% end %>
<%end%>

<p>
<%= link_to "Add a photo ", edit_community_path(@community) -%>
</p>
