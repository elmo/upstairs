<%= simple_form_for([@community, @invitation] ) do |f| %>
  <%= f.error_notification %>
  <%= f.hidden_field :type %>

  <div class="form-inputs">
    <%= f.input :email %>
  </div>

  <div class="form-actions">
    <%= f.button :submit, "Invite" %>
  </div>
<% end %>
