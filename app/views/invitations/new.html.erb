<h1>
<% if @invitation.type == 'UserInvitation' %>
Invite another resident 
<% elsif @invitation.type == 'LandlordInvitation' %>
Invite your landlord
<% elsif @invitation.type == 'ManagerInvitation' %>
Invite a manager to manage 
<% end %>
</h1>
<h2><%= @community.address -%></h2>

<p>It's easy to invite your landlord to join this community. Just enter your landlord's email address, and we'll take care of the rest.</p>

<%= render 'form' %>

<br />

<p><%= link_to 'home', community_path(@community) %></p>
