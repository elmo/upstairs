<div id="map_canvas" style='width: 500px; height: 300px; margin:10px;'></div>
<script>
$(document).ready(function(){
 initialize($("div#map_canvas").get(0));
});

function initialize(element) {
  var latlng = new google.maps.LatLng(<%= @building.latitude-%>, <%= @building.longitude -%>);
   mapCenter = new google.maps.LatLng(<%= @building.latitude-%>, <%= @building.longitude -%>);
  var myOptions = {
    zoom: 18,
    center: mapCenter,
    mapTypeId: google.maps.MapTypeId.HYBRID
  };
  var map = new google.maps.Map(element, myOptions);
}
</script>
