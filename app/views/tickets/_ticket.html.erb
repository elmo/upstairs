<left><%= link_to ticket.title, building_ticket_path(@building, ticket) %>
<% if can?(:manage, ticket) %> 
  <small><%= link_to 'manage', edit_building_ticket_path(@building, ticket) -%></small>
<% end%>
</left>
<middle><%= ticket_severity_badge(ticket) -%></middle>
<right>
<%= ticket_status_badge(ticket)-%> 
</right>
